---
name: wind-finance
description: 通过自然语言查询股票、基金、指数、债券、公告、财经新闻和宏观经济数据。
description_zh: 通过自然语言查询股票、基金、指数、债券、公告、财经新闻和宏观经济数据。
description_en: Query stocks, funds, indices, bonds, announcements, financial news, and macroeconomic data through natural language.
version: "1.0.0"
author: Wind
---

# Wind 金融数据工具指南

# 总述：何时使用 Wind 金融数据 MCP

当用户需要**可追溯的证券、基金、指数、债券、公告、财经新闻或宏观经济结构化数据**时，使用本 Connector。它适合获取事实、行情、指标、筛选结果和时间序列；不要把它当作通用投资建议工具，也不要在未调用工具时编造实时数据或历史数据。

## 路由与调用原则

1. **先识别对象。** 股票、基金、指数、债券、公司公告/新闻、宏观经济数据分别使用对应分类工具；若用户没有给出具体证券、基金或指数，只描述筛选条件，先用 `search_stocks` 或 `search_funds` 取得候选代码。
2. **再匹配时间粒度。** 分钟级日内序列用 `*_quote`；多日 OHLC/K 线用 `*_kline`；只要最新快照或指定指标点用 `*_price_indicators`；技术指标、风险指标、基本面指标分别用 `*_technicals`、`get_risk_metrics`、`*_fundamentals`。
3. **股票主题按意图路由。** 公司身份、上市与行业用 `get_stock_basicinfo`；股本、股东和限售解禁用 `get_stock_equity_holders`；报表、盈利和估值用 `get_stock_fundamentals`；公司重大事项、违规和分红用 `get_stock_events`。
4. **区分信息来源。** 查询上市公司披露文件、定期报告或公告正文时用 `get_company_announcements`；查询媒体报道、市场动态和新闻时用 `get_financial_news`。
5. **宏观数据显式指定 EDB 模式。** 找指标用“仅搜索”；已知 EDB 代码取数用“仅提数”；自然语言找指标并返回数值用“搜索并提数”。取数时传 `beginDate`/`endDate`，或传 `observation`，两者不可同时传。
6. **多标的汇总或计算用 `get_financial_data`。** 将需要的实体、指标、日期、筛选或计算口径写清楚。工具返回空数据或错误时，说明结果并收窄对象、指标或时间范围后再尝试。

# 股票工具

## `search_stocks`

从全市场 A 股中筛选符合条件的股票，返回股票代码列表。适用于用户描述选股条件、但未指定具体股票的场景。

> **适用场景：** 当用户没有给出具体股票代码，而是描述市值、涨跌幅、行业、上市板、连续上涨、资金流入等选股条件时使用。适合先生成候选 A 股代码列表，再继续调用基本面、行情或事件类工具做深入分析。

> **适合的问法：** “帮我找出沪深市场市值超过500亿、最近5日上涨的股票。”

**输入参数**

| 参数       | 类型     | 必填  | 说明        |
| -------- | ------ |:---:| --------- |
| question | string | 是   | 自然语言选股条件。 |

**返回内容**

符合条件的 A 股代码列表。

**调用示例**

```json
{
  "name": "search_stocks",
  "arguments": {
    "question": "筛选沪深市场市值超500亿且近5日上涨的A股"
  }
}
```

---

## `get_stock_basicinfo`

获取股票基本档案及上市信息，包括公司名称、历史曾用名、法定代表人、注册地址、成立日期、经营范围、主营产品、IPO/上市板、行业分类、指数成份归属、概念标签及风险标识等。

> **适用场景：** 查询公司身份、上市和行业档案。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

中英文名称、曾用名、主营业务、上市板、行业、指数成分和概念标签。

**调用示例**

```json
{"name":"get_stock_basicinfo","arguments":{"question":"查询贵州茅台股票的基本档案，包括上市日期、所属行业和主营业务"}}
```

---

## `get_stock_equity_holders`

获取股票股本结构及股东构成，包括总股本、流通股本、自由流通股、前十大股东、实际控制人、机构持股、股东户数和限售解禁等。

> **适用场景：** 查询股票股本、股东结构和限售解禁。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

总股本、流通股本、前十大股东/流通股东、实际控制人、机构持仓和解禁日期数量。

**调用示例**

```json
{"name":"get_stock_equity_holders","arguments":{"question":"查询贵州茅台（600519.SH）的前十大股东及流通A股占比"}}
```

---

## `get_stock_fundamentals`

获取 A 股基本面财务数据，包括盈利能力、资产负债表、利润表、现金流、增长率、杠杆指标、PE/PB/PS 估值及历史分位数等。

> **适用场景：** 查询财务报表、盈利能力和估值。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

营收、净利润、ROE、ROA、毛利率、现金流、负债率、PE/PB/PS 及历史分位数。

**调用示例**

```json
{"name":"get_stock_fundamentals","arguments":{"question":"查询贵州茅台（600519.SH）2024-12-31的ROE、营业收入和净利润"}}
```

---

## `get_stock_quote`

获取指定股票在指定日期范围内的分钟级行情时间序列，包括价格、均价、成交量和换手率等。

> **适用场景：** 查询股票日内分钟级行情。

**输入参数**

| 参数       | 类型     | 必填  | 说明                  |
| -------- | ------ |:---:| ------------------- |
| windcode | string | 是   | Wind 代码或工具支持的名称。    |
| begin    | string | 否   | 开始日期，格式 yyyy-MM-dd。 |
| end      | string | 否   | 结束日期，格式 yyyy-MM-dd。 |

**返回内容**

每分钟价格、均价、成交量和换手率时间序列。

**调用示例**

```json
{"name":"get_stock_quote","arguments":{"begin":"2026-07-27","end":"2026-07-27","windcode":"600519.SH"}}
```

---

## `get_stock_kline`

获取指定股票在给定区间内的日、周或月 K 线，包含开高低收、成交量、成交额、涨跌幅和换手率等。

> **适用场景：** 查询股票日/周/月等 K 线。

**输入参数**

| 参数         | 类型      | 必填  | 说明                    |
| ---------- | ------- |:---:| --------------------- |
| windcode   | string  | 是   | Wind 代码或工具支持的名称。      |
| begin_date | string  | 是   | 开始日期，格式 yyyy-MM-dd。   |
| end_date   | string  | 是   | 结束日期，格式 yyyy-MM-dd。   |
| count      | integer | 否   | 正数向后取、负数向前取。          |
| period     | string  | 否   | 周期：10 日K、11 周K、12 月K。 |
| aftype     | string  | 否   | 0 前复权，1 后复权。          |
| issusp     | string  | 否   | 0 不含停牌，1 包含停牌。        |
| afdate     | string  | 否   | 复权基准日期。               |

**返回内容**

开盘价、最高价、最低价、收盘价、成交量、换手率、涨跌幅和均价。

**调用示例**

```json
{"name":"get_stock_kline","arguments":{"begin_date":"2026-07-20","end_date":"2026-07-27","windcode":"600519.SH","period":"10","aftype":"0","issusp":"1"}}
```

---

## `get_stock_price_indicators`

获取一只或多只股票的最新行情指标快照，可按需返回最新价、涨跌幅、成交量、换手率和估值等指标。

**输入参数**

| 参数       | 类型     | 必填  | 说明                                    |
| -------- | ------ |:---:| ------------------------------------- |
| windcode | string | 是   | 一个或多个股票名称或 Wind 代码；多个代码使用英文逗号 `,` 分隔。 |
| indexes  | string | 否   | 所需行情指标；多个指标使用英文逗号 `,` 分隔。             |

**返回内容**

各股票的最新交易日、交易时间、最新价、涨跌幅、成交量、换手率或指定指标快照。

**调用示例**

```json
{"name":"get_stock_price_indicators","arguments":{"windcode":"600519.SH","indexes":"最新交易日,交易时间,最新成交价,成交量,涨跌幅"}}
```

---

## `get_stock_technicals`

获取股票技术分析与交易特征数据，包括涨跌幅、MACD、KDJ、RSI、BOLL、两融、龙虎榜和涨跌停等。

> **适用场景：** 查询技术分析、两融和龙虎榜。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

多周期涨跌幅、MACD、KDJ、RSI、BOLL、融资融券、龙虎榜和涨跌停状态。

**调用示例**

```json
{"name":"get_stock_technicals","arguments":{"question":"查询贵州茅台（600519.SH）最近20日的收盘价、成交量和MACD"}}
```

---

## `get_risk_metrics`

获取股票风险与收益调整指标，包括 Beta、Jensen Alpha、波动率、夏普比率、最大回撤、VaR 和财务安全指标等。

> **适用场景：** 查询量化风险指标。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

Beta、Jensen Alpha、年化波动率、夏普比率、最大回撤、VaR 和财务安全比率。

**调用示例**

```json
{"name":"get_risk_metrics","arguments":{"question":"查询宁德时代（300750.SZ）过去1年的Beta、年化波动率和最大回撤"}}
```

---

## `get_stock_events`

获取股票公司事件与风险事件，包括融资、并购、管理层和股东变动、限售解禁、股权激励、处罚、诉讼和分红等。

> **适用场景：** 查询资本运作和公司事件。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

IPO、增发、配股、并购、股权激励、风险警示、处罚、诉讼和分红记录。

**调用示例**

```json
{"name":"get_stock_events","arguments":{"question":"查询贵州茅台（600519.SH）的分红派息历史"}}
```

---

# 基金工具

## `search_funds`

从全市场基金中按自然语言条件筛选，返回符合条件的基金代码列表；适用于用户未指定具体基金时的初步筛选。

> **适用场景：** 用户描述筛选条件、未指定基金。

**输入参数**

| 参数       | 类型     | 必填  | 说明        |
| -------- | ------ |:---:| --------- |
| question | string | 是   | 自然语言筛选条件。 |

**返回内容**

符合条件的基金代码列表。

**调用示例**

```json
{"name":"search_funds","arguments":{"question":"筛选股票型基金中近一年收益率超20%的产品"}}
```

---

## `get_fund_info`

获取基金基础档案，包括基金类型、成立日期、管理人、基金经理、投资目标、业绩比较基准和跟踪标的等。

> **适用场景：** 查询基金基础档案。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

基金分类、成立日期、管理人、基金经理和业绩比较基准。

**调用示例**

```json
{"name":"get_fund_info","arguments":{"question":"查询易方达蓝筹精选混合基金的基本档案、基金经理和业绩比较基准"}}
```

---

## `get_fund_holdings`

获取基金持仓和资产配置，包括股票、债券、基金等资产占比、重仓证券、行业配置及 FOF 持仓等。

> **适用场景：** 查询基金资产和重仓配置。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

股票、债券、基金等资产占比，重仓证券、行业配置和 FOF 持仓。

**调用示例**

```json
{"name":"get_fund_holdings","arguments":{"question":"查询易方达蓝筹精选混合基金最近一期重仓股和行业配置"}}
```

---

## `get_fund_holders`

获取基金持有人结构与资金流数据，包括个人和机构持有比例、持有人户数、申购赎回份额及变动情况等。

> **适用场景：** 查询持有人结构与资金流。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

个人/机构持有比例、持有人户数、申购赎回份额和变动率。

**调用示例**

```json
{"name":"get_fund_holders","arguments":{"question":"查询易方达蓝筹精选混合基金的机构持有人比例和持有人户数"}}
```

---

## `get_fund_quote`

获取 ETF、LOF 等场内基金在指定日期范围内的分钟级行情时间序列。

> **适用场景：** 查询 ETF/LOF 分钟行情。

**输入参数**

| 参数       | 类型     | 必填  | 说明                  |
| -------- | ------ |:---:| ------------------- |
| windcode | string | 是   | Wind 代码或工具支持的名称。    |
| begin    | string | 否   | 开始日期，格式 yyyy-MM-dd。 |
| end      | string | 否   | 结束日期，格式 yyyy-MM-dd。 |

**返回内容**

每分钟价格、均价、成交量和换手率。

**调用示例**

```json
{"name":"get_fund_quote","arguments":{"begin":"2026-07-27","end":"2026-07-27","windcode":"510300.SH"}}
```

---

## `get_fund_kline`

获取 ETF、LOF 等场内基金在给定区间内的日、周或月 K 线数据。

> **适用场景：** 查询 ETF/LOF K 线。

**输入参数**

| 参数         | 类型      | 必填  | 说明                    |
| ---------- | ------- |:---:| --------------------- |
| windcode   | string  | 是   | Wind 代码或工具支持的名称。      |
| begin_date | string  | 是   | 开始日期，格式 yyyy-MM-dd。   |
| end_date   | string  | 是   | 结束日期，格式 yyyy-MM-dd。   |
| count      | integer | 否   | 正数向后取、负数向前取。          |
| period     | string  | 否   | 周期：10 日K、11 周K、12 月K。 |
| aftype     | string  | 否   | 0 前复权，1 后复权。          |
| issusp     | string  | 否   | 0 不含停牌，1 包含停牌。        |
| afdate     | string  | 否   | 复权基准日期。               |

**返回内容**

开高低收、成交量、换手率和涨跌幅序列。

**调用示例**

```json
{"name":"get_fund_kline","arguments":{"begin_date":"2026-07-20","end_date":"2026-07-27","windcode":"510300.SH","period":"10","aftype":"0","issusp":"1"}}
```

---

## `get_fund_price_indicators`

获取一只或多只场内基金的最新行情指标快照，包括价格、成交量、换手率、IOPV 和折溢价率等。

> **适用场景：** 查询场内基金当前指标。

**输入参数**

| 参数       | 类型     | 必填  | 说明                                    |
| -------- | ------ |:---:| ------------------------------------- |
| windcode | string | 是   | 一个或多个基金名称或 Wind 代码；多个代码使用英文逗号 `,` 分隔。 |
| indexes  | string | 否   | 所需行情指标；多个指标使用英文逗号 `,` 分隔。             |

**返回内容**

最新价、成交量、换手率、IOPV、折溢价率或指定指标。

**调用示例**

```json
{"name":"get_fund_price_indicators","arguments":{"windcode":"510300.SH","indexes":"最新交易日,交易时间,最新成交价,成交量,涨跌幅,IOPV"}}
```

---

## `get_fund_performance`

获取基金业绩表现、排名及交易数据，包括多周期收益、同类排名、风险调整指标和 ETF/LOF 折溢价等。

> **适用场景：** 查询基金收益、排名和风险调整业绩。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

多周期收益、同类排名、Alpha、Beta、夏普、信息比率和最大回撤。

**调用示例**

```json
{"name":"get_fund_performance","arguments":{"question":"查询易方达蓝筹精选混合近一年收益率、同类排名和最大回撤"}}
```

---

## `get_fund_financials`

获取基金财务与分红数据，包括利润、净值、利息收入、公允价值变动、费用和分红等。

> **适用场景：** 查询基金财务和分红。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

基金利润、净值、利息收入、公允价值变动、费用和分红金额。

**调用示例**

```json
{"name":"get_fund_financials","arguments":{"question":"查询易方达蓝筹精选混合基金2024年年报的管理费、托管费和分红情况"}}
```

---

## `get_fund_company_info`

获取基金管理公司信息，包括公司档案、基金经理团队、在管规模、产品数量和资产配置等。

> **适用场景：** 查询基金管理公司。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

公司档案、基金经理团队、在管规模、基金数量和资产配置。

**调用示例**

```json
{"name":"get_fund_company_info","arguments":{"question":"查询易方达基金管理有限公司的成立日期、管理规模和基金经理团队情况"}}
```

---

# 指数工具

## `get_index_basicinfo`

获取指数基本档案，包括指数名称、发布机构、基日、基点、计算方法、成分数量和指数类别等。

> **适用场景：** 查询指数档案。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

指数名称、发布机构、基日、基点、计算方法和成分数量。

**调用示例**

```json
{"name":"get_index_basicinfo","arguments":{"question":"查询沪深300指数的基本信息，包括发布机构、基日和成份股数量"}}
```

---

## `get_index_quote`

获取指定指数在指定日期范围内的分钟级行情时间序列。

> **适用场景：** 查询指数分钟行情。

**输入参数**

| 参数       | 类型     | 必填  | 说明                  |
| -------- | ------ |:---:| ------------------- |
| windcode | string | 是   | Wind 代码或工具支持的名称。    |
| begin    | string | 否   | 开始日期，格式 yyyy-MM-dd。 |
| end      | string | 否   | 结束日期，格式 yyyy-MM-dd。 |

**返回内容**

每分钟价格、均价、成交量和换手率。

**调用示例**

```json
{"name":"get_index_quote","arguments":{"begin":"2026-07-27","end":"2026-07-27","windcode":"000300.SH"}}
```

---

## `get_index_kline`

获取指定指数或板块在给定区间内的日、周或月 K 线数据。

> **适用场景：** 查询指数或板块 K 线。

**输入参数**

| 参数         | 类型      | 必填  | 说明                    |
| ---------- | ------- |:---:| --------------------- |
| windcode   | string  | 是   | Wind 代码或工具支持的名称。      |
| begin_date | string  | 是   | 开始日期，格式 yyyy-MM-dd。   |
| end_date   | string  | 是   | 结束日期，格式 yyyy-MM-dd。   |
| count      | integer | 否   | 正数向后取、负数向前取。          |
| period     | string  | 否   | 周期：10 日K、11 周K、12 月K。 |
| aftype     | string  | 否   | 0 前复权，1 后复权。          |
| issusp     | string  | 否   | 0 不含停牌，1 包含停牌。        |
| afdate     | string  | 否   | 复权基准日期。               |

**返回内容**

开高低收、成交量、换手率和涨跌幅序列。

**调用示例**

```json
{"name":"get_index_kline","arguments":{"begin_date":"2026-07-20","end_date":"2026-07-27","windcode":"000300.SH","period":"10","aftype":"0","issusp":"1"}}
```

---

## `get_index_price_indicators`

获取一只或多只指数的最新行情指标快照，包括点位、涨跌幅、成交量和估值等。

> **适用场景：** 查询指数当前指标。

**输入参数**

| 参数       | 类型     | 必填  | 说明                                    |
| -------- | ------ |:---:| ------------------------------------- |
| windcode | string | 是   | 一个或多个指数名称或 Wind 代码；多个代码使用英文逗号 `,` 分隔。 |
| indexes  | string | 否   | 所需行情指标；多个指标使用英文逗号 `,` 分隔。             |

**返回内容**

最新价、涨跌幅、成交量、市盈率、市净率或指定指标。

**调用示例**

```json
{"name":"get_index_price_indicators","arguments":{"windcode":"000300.SH","indexes":"最新交易日,交易时间,最新成交价,成交量,涨跌幅"}}
```

---

## `get_index_technicals`

获取指数技术分析数据，包括多周期涨跌幅、趋势、动量、超买超卖和波动指标等。

> **适用场景：** 查询指数技术指标。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

多周期涨跌幅、趋势、动量、超买超卖和波动指标。

**调用示例**

```json
{"name":"get_index_technicals","arguments":{"question":"查询沪深300指数的技术指标，包括MACD、RSI和20日涨跌幅"}}
```

---

## `get_index_fundamentals`

获取指数成分股加权基本面数据，包括营收、净利润、现金流、盈利能力及 PE/PB/PS 等。

> **适用场景：** 查询指数成分股加权基本面。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

加权营收、净利润、现金流、盈利能力及 PE/PB/PS。

**调用示例**

```json
{"name":"get_index_fundamentals","arguments":{"question":"查询沪深300指数2024年的基本面数据，包括PE、PB和成份股加权营收"}}
```

---

# 债券工具

## `get_bond_basicinfo`

获取债券基本档案，包括债券分类、发行日期、发行规模、票面利率、期限、兑付安排和评级等。

> **适用场景：** 查询债券档案。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

债券分类、发行日期、规模、发行价、票面利率、期限和兑付安排。

**调用示例**

```json
{"name":"get_bond_basicinfo","arguments":{"question":"查询国债2601的基本信息，包括发行日期、票面利率和期限"}}
```

---

## `get_bond_market_data`

获取债券行情与估值数据，包括交易行情、估值、收益率、久期、凸性和利差等。

> **适用场景：** 查询债券行情与估值。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

交易行情、估值、溢价、久期、凸性和利差。

**调用示例**

```json
{"name":"get_bond_market_data","arguments":{"question":"查询国债2601的行情数据，包括久期、凸性和收益率"}}
```

---

## `get_bond_issuer_info`

获取债券发行人基本信息，包括主体名称、注册地址、行业、股权结构和企业背景等。

> **适用场景：** 查询债券发行主体。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

主体名称、注册地址、行业、股权结构和企业背景。

**调用示例**

```json
{"name":"get_bond_issuer_info","arguments":{"question":"查询国债2601发债主体的公司信息，包括注册地址和行业分类"}}
```

---

## `get_bond_financial_data`

获取债券发行人财务数据，包括营收、利润、资产、负债和偿债能力等主体层面指标。

> **适用场景：** 查询发行主体财务。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

营收、利润、资产、负债和主体层面财务表现指标。

**调用示例**

```json
{"name":"get_bond_financial_data","arguments":{"question":"查询国债2601发债主体2024年营业收入和净利润"}}
```

---

# 信息与宏观工具

## `get_company_announcements`

检索上市公司官方公告与监管披露文件，返回定期报告、临时公告和监管文件等结果。

> **适用场景：** 检索官方公告与监管披露。

**输入参数**

| 参数    | 类型      | 必填  | 说明            |
| ----- | ------- |:---:| ------------- |
| query | string  | 是   | 必须包含对象和时间范围。  |
| top_k | integer | 否   | 返回数量，范围 1–20。 |

**返回内容**

定期报告、临时公告、监管文件的标题、日期、来源和文档片段。

**调用示例**

```json
{
  "name": "get_company_announcements",
  "arguments": { "top_k": 3, "query": "贵州茅台最新公告" }
}
```

---

## `get_financial_news`

检索第三方财经媒体报道，返回与证券、行业、公司或宏观主题相关的新闻结果。

> **适用场景：** 检索第三方财经媒体报道。

**输入参数**

| 参数    | 类型      | 必填  | 说明            |
| ----- | ------- |:---:| ------------- |
| query | string  | 是   | 必须包含对象和时间范围。  |
| top_k | integer | 否   | 返回数量，范围 1–20。 |

**返回内容**

新闻标题、发布日期、媒体来源和相关片段。

**调用示例**

```json
{
  "name": "get_financial_news",
  "arguments": { "top_k": 3, "query": "贵州茅台最新一周的新闻" }
}
```

---

## `get_financial_data`

根据自然语言请求执行通用金融数据查询、跨实体汇总、筛选、排名或派生计算。

> **适用场景：** 执行跨实体聚合计算。

**输入参数**

| 参数       | 类型     | 必填  | 说明                        |
| -------- | ------ |:---:| ------------------------- |
| question | string | 是   | 自然语言查询，应包含对象、指标和需要时的时间范围。 |

**返回内容**

行业合计、加权平均、排名或其他结构化派生计算结果。

**调用示例**

```json
{
  "name": "get_financial_data",
  "arguments": { "question": "查询中国A股市场过去一年的平均成交量" }
}
```

---

## `natural_language_get_edb_data`

根据自然语言问句或 EDB 指标代码，从 Wind EDB 经济数据库中搜索指标并获取时间序列数据。支持仅搜索、仅提数、搜索并提数三种模式。

> **适用场景：** 当用户查询宏观经济指标或 Wind EDB 时间序列时使用。仅想找指标用“仅搜索”；已知 EDB 代码直接取数用“仅提数”；自然语言找指标并返回数值用“搜索并提数”。取数时显式传日期或期数。

> **适合的问法：** “帮我搜索中国GDP相关的Wind EDB指标。”

**输入参数**

| 参数            | 类型     | 必填  | 说明                                     |
| ------------- | ------ |:---:| -------------------------------------- |
| executionMode | string | 是   | 仅搜索、仅提数或搜索并提数。                         |
| question      | string | 是   | 搜索描述；仅提数时填 EDB 指标代码，多个代码使用英文逗号 `,` 分隔。 |
| beginDate     | string | 否   | 开始日期，格式 `yyyyMMdd`；与 `observation` 互斥。 |
| endDate       | string | 否   | 结束日期，格式 `yyyyMMdd`；与 `observation` 互斥。 |
| observation   | string | 否   | 最近 N 期或 `all`；与日期范围互斥。                 |

**返回内容**

指标代码、名称、频率、单位和时间序列数值。

**调用示例**

```json
{
  "name": "natural_language_get_edb_data",
  "arguments": {
    "question": "中国GDP",
    "executionMode": "仅搜索"
  }
}
```

---
