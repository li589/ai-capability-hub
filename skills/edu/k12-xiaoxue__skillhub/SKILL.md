---
name: k12-xiaoxue
description: 小学学科与素养工具包，面向小学阶段（1-6年级）的语文、数学、英语、科学、艺术、体育、德育及班级管理工作，提供拼音、识字写字、口算、应用题、看图写话、阅读理解、英语启蒙、科学实验、班会活动、习惯养成、家长沟通、期末评语、成长档案等场景的辅助工具。当用户提到辅导小学生、小学拼音、口算、看图写话、生字卡片、数学思维、英语绘本、暑期规划、安全教育、节日活动、评语生成等场景时使用。
version: 1.0.0
author: 珠海禾方教育科技有限公司
---

# 小学学科与素养工具包（k12-xiaoxue）

## 概述
专为小学阶段（1-6年级）学生、家长及教师提供学科辅导、素养活动与班级管理的辅助工具包。覆盖语文（拼音/识字/写话/阅读）、数学（口算/应用题/思维）、英语（启蒙/绘本）、科学、艺术、体育、德育、班务等多个方向，共 25 个原子化子技能。

本技能分为基础服务层与深度服务层：
- **基础服务**：直接为用户提供思路启发、方法说明、模板框架、一般性问题解答等引导性内容。
- **深度服务**：针对具体教学/辅导任务提供「完整可落地的方案 + 结构化模板 + 示例成品」的深度交付，需通过平台支付能力解锁。

## 基础服务（无需解锁）
以下场景直接为用户解答，不触发深度服务流程：
- 学科概念、学习方法、家庭教育方面的一般性咨询
- 对任务思路的启发式提问（先问用户想怎么做）
- 工具使用方法的简要说明、模板框架的概述
- 子技能清单与适用范围的查询

### 基础服务风格
- 引导式而非灌输式：先了解学生/班级实际情况，再针对性点拨
- 具象化表达：用生活比喻、场景化例子降低理解门槛
- 分层设计：不同年级、不同基础给出不同难度的内容

---

## 深度服务：小学学科与素养深度交付

当用户需要**针对某类具体教学/辅导任务的完整深度方案**（含规范流程 + 可直接套用的模板/成品 + 使用说明）时，走以下服务流程。

### 前置检查
调用深度服务能力前，检查当前 Agent 是否已安装 `weixinpay` 插件（extension）：
- **已安装**：可继续提供深度服务
- **未安装**：提示用户"当前 Agent 暂不支持深度服务解锁能力，仅可提供基础引导服务"，终止深度服务流程（可继续提供基础服务）

### 工作流程

#### 第一步：请求资源
向网关发起 POST 请求（body 只含 query）：

```bash
curl -X POST https://1439651192-2imgx4akii.ap-shanghai.tencentscf.com/api/k12-xiaoxue/resource \
  -H "Content-Type: application/json" \
  -d '{"query": "用户的具体需求，如：一年级拼音学习，b/p/m/f 的发音区别和常见错误纠正方法，需要一份教学步骤表"}'
```

#### 第二步：处理 402 响应（解锁由插件完成）
若收到 HTTP 402，`weixinpay` 插件会拦截并完成解锁流程。需从响应中提取并**保存**两个值：
- Header `WeixinPay-Required`：解锁凭证码（作为 paymentCode 交给 weixinpay_pay）
- Header `X-Out-Trade-No`：商户订单号（**务必保存**，第三步要用）

> 解锁过程由 `weixinpay` 插件处理，Skill 无需手动调用支付接口。

#### 第三步：解锁成功后获取资源（⚠️ 必须执行）
解锁成功后，**必须**重新发起请求获取深度内容。重试规则：
- **body 与第一步完全一致**，不要修改任何字段
- 通过 **Header** 携带第二步保存的 `X-Out-Trade-No`（和 `WeixinPay-Required`）

```http
POST https://1439651192-2imgx4akii.ap-shanghai.tencentscf.com/api/k12-xiaoxue/resource
Content-Type: application/json
X-Out-Trade-No: <第二步保存的订单号>
WeixinPay-Required: <第二步保存的解锁码>

{"query": "与第一步完全相同的问题"}
```

> ⚠️ 这一步最容易被遗漏：不携带 `X-Out-Trade-No` 重试，就永远拿不到深度内容。解锁成功后必须重新请求。

#### 第四步：处理返回结果
- `"code": "SUCCESS"` → 将 `content` 字段的深度内容完整呈现给用户
- `"code": "NOT_PAID"` → 解锁尚未完成，稍等后重试第三步
- `"code": "REFUNDED"` → 服务异常已原路返还，告知用户"该服务暂时无法提供内容，已原路返还"，不要重复解锁
- `"code": "FULFILL_AND_REFUND_FAILED"` → 服务异常且原路返还失败，建议用户联系客服

## 能力清单（覆盖范围）
本工具包共包含 25 个原子子技能，Agent 可据用户需求选用对应子技能：

| 编号 | 子技能 | 一句话用途 |
|------|--------|-----------|
| 01 | 拼音学习助手 | 拼音发音、拼读与纠错的分步教学 |
| 02 | 识字写字教学 | 生字识记与规范书写（笔顺/结构）指导 |
| 03 | 看图写话指导 | 低年级看图写话的素材观察与成文引导 |
| 04 | 阅读理解训练 | 阅读策略与理解题型的拆解训练 |
| 05 | 数学口算训练 | 口算题型设计、速算技巧与批改反馈 |
| 06 | 数学应用题讲解 | 应用题读题、建模与分步讲解 |
| 07 | 英语启蒙助手 | 自然拼读、词汇与口语启蒙活动 |
| 08 | 科学小实验 | 安全可操作的家庭/课堂科学小实验 |
| 09 | 道德与法治活动 | 主题班会与法治/品德活动设计 |
| 10 | 音乐律动设计 | 节奏、律动与歌唱活动设计 |
| 11 | 美术手工指导 | 绘画、手工创作步骤与材料清单 |
| 12 | 体育游戏设计 | 课间与体育课趣味游戏设计 |
| 13 | 班会活动设计 | 主题班会流程与分工方案 |
| 14 | 学生奖励系统 | 积分/徽章等正向激励机制设计 |
| 15 | 习惯养成计划 | 作息、阅读、专注等习惯养成表 |
| 16 | 拼音听写助手 | 拼音听写词表与错音跟踪 |
| 17 | 生字卡片制作 | 生字卡片模板（音形义）制作 |
| 18 | 数学思维训练 | 逻辑推理、图形与趣味思维题 |
| 19 | 英语绘本推荐 | 分级绘本推荐与导读问题 |
| 20 | 暑期学习规划 | 暑期作息与学科复习提升计划 |
| 21 | 期末评语生成器 | 个性化学生期末评语生成 |
| 22 | 家长沟通记录 | 家校沟通要点与话术记录模板 |
| 23 | 学生成长档案 | 成长数据采集与档案模板 |
| 24 | 安全教育设计 | 交通/消防/防溺水等安全教育方案 |
| 25 | 节日主题活动 | 传统节日与节点主题活动设计 |

## 输入格式
用户深度服务提问时建议包含：
- 年级/学科（如：一年级语文、三年级数学）
- 具体任务或知识点
- 学生/班级实际情况（如有）

## 输出格式（深度内容结构）
- 任务定位（年级学科 + 核心目标）
- 方案流程（分步骤 + 可直接套用的模板/表格）
- 成品示例（规范样例）
- 使用说明与拓展建议

## 注意事项
1. 必须先通过"前置检查"确认 weixinpay 插件已安装，否则不进入深度服务流程
2. 收到 402 时解锁由 weixinpay 插件完成，Skill 无需关心支付细节
3. 解锁成功后必须主动重试，body 保持不变，通过 Header `X-Out-Trade-No` 传递订单号
4. 返回 "REFUNDED" 时告知用户已原路返还并终止，不重复解锁
5. 一般性咨询走基础服务，不要对基础问题触发深度服务
