python-pptx>=1.0.0
PyMuPDF>=1.24.0
Pillow>=10.0.0
matplotlib>=3.8.0
numpy>=1.24.0          # imported DIRECTLY by designed_charts.distribution/radar. It arrives
                       # transitively with matplotlib today, but a direct import belongs in
                       # the manifest rather than resting on someone else's dependency.

# Optional (feature-gated; scripts degrade gracefully without them):
#   cairosvg>=2.7      — SVG icon rasterization fallback when Chrome/Chromium is absent
#   LibreOffice (system) — render_deck.sh PNG render loop · ingest.py office (doc -> PDF)
#   Chrome/Chromium (system) — primary SVG rasterizer for icons.py
#   python-docx>=1.0   — ingest.py doctext (Word .docx exact text/tables)
#   openpyxl>=3.1      — ingest.py sheet (Excel .xlsx exact rows)
#   ffmpeg/ffprobe (system) — ingest.py frames (video keyframe sampling)
