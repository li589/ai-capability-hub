# 00 主链路检查报告

> 由 /e2e-self-check 自动生成（独立子代理视角） | 生成时间: {YYYY-MM-DD HH:mm}

## 检查目标

端到端验证核心用户故事的主链路实现与 spec/plan 的一致性——先确认主干逻辑，再分而治之。

## 主链路清单

<!-- 按 spec.md 的 P1（必要时含 P2）User Story 逐条走查 -->

### 链路 1：{User Story 标题}（{P1}）

**期望路径（依据 plan.md）**：{入口 → 服务 → 数据层 → 返回，写明关键类/组件名}

**实际走查**：

| 环节 | 代码位置 | 与 plan 一致 | 说明 |
|------|----------|--------------|------|
| 入口 | {path}:{line} | 是/否 | {Controller/页面组件等} |
| 业务层 | {path}:{line} | 是/否 | |
| 数据层 | {path}:{line} | 是/否 | |
| 返回/渲染 | {path}:{line} | 是/否 | |

**验收场景比对（Given-When-Then）**：

| 场景 | 实现状态 | 证据 |
|------|----------|------|
| Given {..} When {..} Then {..} | 已实现/未实现/部分实现 | {path}:{line} |

**结论**：{PASS / 问题描述}

## 发现的问题

| # | 级别 | 问题 | 证据（文件:行号） | 违反规则 |
|---|------|------|------------------|----------|
| 1 | {BLOCKER/MAJOR/MINOR} | {描述} | {path}:{line} | {规则编号或"-"} |

## 主链路判定

```
main_chain_result: {PASS | FAIL}
```

{主链路 FAIL 时，模块分治检查暂停，先修复主干。}
