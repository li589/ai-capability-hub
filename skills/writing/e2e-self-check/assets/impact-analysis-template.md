# 影响面分析报告

> 自动生成时间：{timestamp}
> 检查范围：{diff_range}

## 变更符号清单

| 文件 | 符号 | 变更类型 |
|------|------|----------|
| `path/to/File.java` | `methodName(params)` | 新增/修改/删除 |

## 影响面追溯结果

### Callers（调用方受影响）

| 受影响文件 | 行号 | 调用的变更符号 | 影响说明 | 是否已同步修改 |
|-----------|------|--------------|----------|--------------|
| `path/to/Caller.java` | L42 | `SomeService.method()` | 参数签名变更需适配 | ❌ 未修改 |

### Callees（被调用方兼容性）

| 变更方法 | 调用的方法 | 文件 | 兼容性判定 |
|----------|-----------|------|-----------|
| `ServiceImpl.create()` | `Mapper.insert()` | `Mapper.xml` | ✅ 兼容 / ❌ 不兼容 |

### 数据模型同步

| Entity 变更字段 | 关联 Request | 关联 Response | 关联 Convert | 同步状态 |
|----------------|-------------|--------------|-------------|---------|
| `User.phone` | `CreateUserRequest` | `QueryUserResponse` | `UserConvert` | ✅ / ❌ |

### 跨模块影响

| 变更模块 | 受影响模块 | 影响路径 | 状态 |
|----------|-----------|---------|------|
| `api` | `service` | `Facade 接口签名变更` | ✅ 已适配 / ❌ 未适配 |

## 统计

- 变更符号数：{count}
- 受影响文件数（未在 diff 中）：{count}
- 需同步修改但未修改：{count}（如 > 0 则产出 BLOCKER/MAJOR）

## 遗留风险

> 列出因代码量过大或跨仓库而无法完全追溯的部分，建议人工复核。
