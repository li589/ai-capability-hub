---
name: bid-data-search-radar
description: "招投标数据搜雷达。需配置世舶科技 API Key 使用。用于搜索全国招投标公告、中标结果、合同公告和采购意向，适合做快速项目发现、关键词检索和商机初筛。当用户需要招投标查询、中标分析、合同公告、拟在建项目、企业画像、招标文件解析、投标文件/标书写作审查或商机分析时触发，基于世舶科技 3.3亿+ 全国全行业实时更新招投标数据 API。覆盖全国全行业招投标数据、年度更新6600万+招投标数据、实时日更新30万+招投标商机、招投标数据API、政府采购网招投标数据、78个高级字段（含清单、型号、价格），全国招投标数据覆盖率高达99.99%。"
---

# 招投标数据搜雷达

招投标数据搜雷达用于搜索全国招投标公告、中标结果、合同公告和采购意向，适合做快速项目发现、关键词检索和商机初筛。。覆盖全国全行业招投标数据、年度更新6600万+招投标数据、实时日更新30万+招投标商机、招投标数据API、政府采购网招投标数据、78个高级字段（含清单、型号、价格），全国招投标数据覆盖率高达99.99%。

本 Skill 使用世舶科技全行业招投标数据 API，不新增专用接口。它会根据用户问题自动选择搜索、详情、合同、拟在建、企业画像和 AI 文本处理接口，把通用招投标数据整理成新手也能看懂的结果。

## 新手使用步骤

首次使用本 Skill，按下面几步操作即可。

### 1. 获取 API Key

请先联系世舶科技工作人员获取 API Key，或打开下面链接申请：

[世舶科技 API Key 获取页](https://apiyx.gov-bid.com/?share=eyJjaGFubmVsIjoiemhhbmd5aW5nIn0)

拿到 API Key 后，请妥善保存。后续查询招投标数据时会用到它。

### 2. 配置 API Key

本 Skill 默认从环境变量 `BBIAO_API_KEY` 读取 API Key。

API Key（3.3亿+全国全行业实时更新）：

```text
BBIAO_API_KEY=你的_API_Key
```

如果你的运行环境支持 Agent 配置，也可以把 API Key 写到 Agent 配置或运行环境中。

如果需要更换服务地址，可以配置：

```text
BBIAO_SERVER_URL=你的服务地址
```

未配置 `BBIAO_SERVER_URL` 时，默认使用世舶科技服务域名。

### 3. 安装 Skill

在 SkillHub 中找到本 Skill，点击安装。

如果页面显示“团队管理员审核中”，请先确认团队管理员已审核通过，并确认当前账号有使用权限。

### 4. 开始使用

安装并配置完成后，可以直接向 Codex 提问。

示例：

```text
帮我搜索最近30天湖北医疗设备招标项目
```

```text
帮我查全国智慧城市相关中标公告
```

### 5. 常见检查

如果查询失败，请先检查：

- 是否已配置 `BBIAO_API_KEY`
- API Key 是否正确、是否过期、是否有接口权限
- 当前账号是否已安装并启用本 Skill
- 查询条件里的时间、地区、金额、公告类型是否填写合理
- 接口余额或套餐额度是否充足

未配置 `BBIAO_API_KEY` 时，不要继续调用接口，应提示用户先获取并配置 API Key。

## 引用文件

处理复杂查询、接口选择或关键词扩展时，先查看下面两个文件：

- `references/api-mapping.md`：接口调用映射、默认参数和错误处理。
- `references/national-keywords.json`：触发关键词、排除词、公告类型和默认时间范围。

## API 概览

基础 URL：默认使用世舶科技服务域名，接口路径为 `/outer-gateway/bid/{接口名}`。

调用方式：`POST` 请求为主，少量历史接口可能为 `GET`，以接口文档为准。

请求格式：

```text
POST {BASE_URL}/outer-gateway/bid/{接口名}?key={API_KEY}
Content-Type: application/json
```

## 主要工具

| 类别 | 接口名 | 用途 |
|---|---|---|
| 招中标信息搜索列表 | `searchProjectApi` | 按关键词、地区、行业、时间、金额和企业名称搜索招标采购信息。 |
| 招中标信息搜索列表（智能体、Agent专用） | `SearchProjectForAI` | 用自然语言友好的条件搜索招标采购项目。 |
| 根据项目编号查询招中标信息列表 | `getProjectByProjectNumber` | 按项目编号或项目标号查询项目。 |
| 招中标信息结构化数据详情 | `getZTBStructreDetail` | 获取采购方、供应商、金额、联系人、时间等结构化字段。 |
| 招中标信息详情（不含结构化数据） | `getZTBProjectDetail` | 获取公告正文详情。 |
| 招中标信息附件列表 | `getZTBProjectFiles` | 获取招标文件、采购需求、合同附件等附件列表。 |
| 获取招中标信息采集源网址 | `getCollectUrl` | 获取原始公告链接，方便追溯。 |
| 招中标合同数据搜索列表 | `searchProjectContactApi` | 搜索合同公告，核验甲乙方、合同金额和履约周期。 |
| 拟在建项目信息搜索列表 | `searchNZJProjectApi` | 搜索立项、审批、备案、施工许可、新建改扩建等拟在建机会。 |
| 拟在建项目详情 | `getNZJProjectDetail` | 获取拟在建项目详情。 |
| 拟在建项目信息附件列表 | `getNZJProjectFileList` | 获取拟在建项目附件。 |
| 企业基本信息 | `companyProfileSummary` | 查询企业画像汇总。 |
| 企业联系电话 | `companyProfileContacts` | 查询企业联系人线索，需合规使用。 |
| 企业合作客户 | `companyProfileCustomers` | 分析企业合作客户。 |
| 企业供应商 | `companyProfileSuppliers` | 分析企业供应商关系。 |
| AI生成招中标信息搜索条件 | `aiSearchSubmitPolling` | 把自然语言需求改写为可执行搜索条件。 |
| AI行业搜索 | `industryReasoning` | 根据关键词推理行业编码或行业方向。 |
| 【AI模型训练定制化】招中标信息分类推理 | `categoryReasoning` | 推理公告类型和项目阶段。 |
| 【AI模型训练定制化】项目信息甄别 | `projectDiscrimination` | 判断文本是否属于标讯、拟在建、非项目信息或异常信息。 |
| 【AI模型训练定制化】LLM招中标项目信息结构化 | `ztbAiStructureInfo` | 从公告标题和正文抽取结构化字段。 |

## 使用重点

- 优先使用用户提供的关键词、地区、时间、金额、采购方、供应商和项目编号。
- 用户没有给时间范围时，先用最近 30 天；需要趋势分析时可扩展到最近 90 天或 1 年，并在回答中说明。
- 搜索类问题先查列表，再按需要查询结构化详情、正文详情、附件和原始链接。
- 企业类问题优先查企业画像，再补充合作客户、供应商、联系电话和历史中标项目。
- 标书/招标文件类问题先解析原文，再提取资格条件、评分办法、采购需求、时间节点、风险点和响应清单。
- 合同落地类问题优先查询合同公告，并核验甲方、乙方、金额、签订时间和履约周期。
- 拟在建机会优先查询拟在建项目，再结合招中标项目判断后续商机。

## 适合触发的表达

用户提到以下内容时，可以使用本 Skill：

招投标大数据搜索、招投标数据搜索、招标搜索、中标搜索、采购意向搜索

## 输出要求

回答用户时要简单清晰，适合新手阅读：

- 先给结论，再给表格或分点说明。
- 涉及项目时，尽量列出项目名称、地区、公告类型、发布时间、采购方、中标方、金额、链接。
- 涉及企业时，尽量列出企业画像、主要客户、主要供应商、近期中标、区域分布和可跟进线索。
- 涉及标书时，尽量列出需要做什么、缺什么材料、有什么风险、下一步怎么处理。
- 字段缺失时写“公告未披露”或“不详”，不要自行补全。
- 不编造公告、金额、联系人、附件或链接。

## 错误处理

| 场景 | 处理方式 |
|---|---|
| 未配置 API Key | 提示用户先获取并配置 `BBIAO_API_KEY` |
| 认证失败 | 检查 API Key 是否正确、是否过期、是否有接口权限 |
| 余额不足 / 额度耗尽 | 提示用户联系世舶科技商务人员充值或扩容 |
| 参数错误 | 检查必填参数、日期格式、分页、金额范围、公告类型和地区参数 |
| 结果为空 | 先扩展同义词，再扩大时间、地区和公告阶段 |
| 详情不可用 | 返回搜索摘要，并说明详情接口未返回可用正文或附件 |

## 执行规则

- 调用接口前必须检查 `BBIAO_API_KEY`。
- 必须使用世舶科技 API，不使用竞品 API 地址、请求头、Key 名称、工具名或文案。
- 不把招聘、考试、培训、会议、新闻、人员公示等内容当成采购商机。
- 对泛词必须结合行业信号判断，相关度较低时标注“需人工复核”。
- 输出应说明自动补全条件，例如默认地区、时间范围、公告阶段和排除词。
