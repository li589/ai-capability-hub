# STATE.md — {{TASK_TITLE}} 持久记忆

> 这是 LOOP 的「持久记忆(Memory)」要素。
> 模型每次会话之间会完全遗忘,但这个文件不会。
> **仓库记得,即使模型不记得**。

---

## 状态标记(LOOP 在顶部更新)

- **当前状态**:`[INIT]`
- **最后更新**:尚未启动
- **已运行轮数**:`0 / {{ROUNDS_BUDGET}}`

> 状态可能的值:
> - `[INIT]` 还没跑过
> - `[RUNNING]` 正在迭代中
> - `[NEEDS_HUMAN_REVIEW]` A 类自动指标都过了,等用户做人工验收(仅 HUMAN_REVIEW_NEEDED=yes 时出现)
> - `[DONE]` 任务完成
> - `[STUCK]` 连续 3 轮无进展
> - `[BLOCKED]` 触碰禁区
> - `[TOO_BIG]` 单轮改动过大
> - `[BUDGET_OUT]` 超过 {{ROUNDS_BUDGET}} 轮预算
> - `[SYNTAX_BROKEN]` 修改导致语法/编译错误且当轮无法修复

---

## 当前任务概要(LOOP 必读)

**任务**:{{TASK_TITLE}}

**问题描述**:{{PROBLEM_DESC}}

**LOOP 模式**:{{LOOP_PATTERN}}

详见 GOAL.md。

---

## 当前假设(下一轮要验证什么)

- 假设:`[尚未开始]` —— 首轮 LOOP 应该先做"诊断/探查":
  通读项目结构、定位关键文件、产出第一个改造方案的草案,**不要直接改代码**。
- 计划下一步:`首轮做诊断与设计,不改代码`
- 预期观察:`产出对项目的理解和第一个改造方案`

---

## 已经排除的方向(避免反复尝试)

> 防止"空转":LOOP 反复试同一个无效方案。
> 每次失败的尝试都要写进来,下次不再走这条路。

(空)

---

## 设计决策记录(改造过程中的关键决定)

> 用于记录"为什么这么设计",方便人工 review 和未来回滚。

(空)

---

## 运行日志(每轮追加,最新在最上)

> 格式模板:
>
> ```
> ### 轮次 N — YYYY-MM-DD HH:MM
> - 本轮目标:[这一轮想验证或修复什么]
> - 执行动作:[修改了哪些文件、跑了什么命令]
> - 观察结果:[关键摘要,不要复制大段输出]
> - 是否进展:[YES/NO,NO 必须写明原因]
> - 下一步假设:[基于观察的新假设]
> - 自检结果:[子 Agent 返回 APPROVED/REJECTED + 理由]
> ```

(尚无运行记录)

---

## 已完成清单(归档区)

> LOOP 达成 `[DONE]` 后,把这次任务的成果摘要搬到这里。

(空)
