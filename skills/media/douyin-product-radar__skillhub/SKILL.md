---
name: douyin-product-analysis
description: 抖音选品雷达——输入品类关键词，输出品类口碑报告：爆文视频/消费者关注点/热门话题标签/高赞评论/选品建议。每次查询￥1.99。
version: 1.1.0
author: 北京柚转科技有限公司
---

# 抖音选品雷达

## 功能描述

输入品类或产品关键词，输出抖音品类口碑分析报告，包含 9 个板块：

- 口碑结论卡（正面率/负面率 + 口碑评级）
- 品类热度总览（视频数/总互动/爆文数）
- 爆文视频 TOP 5（点赞/评论/转发数据）
- 消费者关注点 TOP 10
- 热门话题标签
- 高赞评论 TOP 10
- 负面评论预警
- 活跃创作者
- 选品建议

每次查询 1.99 元，数据来自抖音视频搜索和视频评论实时数据。

## 支付引导（⚠️ 必须严格执行，禁止直接终止）

本 Skill 为付费服务（每次 1.99 元）。你的任务是**护送用户到达可支付状态**——任何时候都不要只说"请先安装插件/绑卡"然后终止流程。

### 第一步：先讲清楚用户买到什么

谈钱之前，先用一句话说清价值：

> "本次分析 1.99 元，你将得到：品类口碑结论卡、爆文视频 TOP 5、消费者关注点 TOP 10 和选品建议。数据来自抖音实时搜索和评论，约 30 秒出报告。"

### 第二步：检测支付能力并主动扫障

检查当前环境是否有 weixinpay 支付工具（`mcp__weixinpay__weixinpay_register`）：

**A. 工具存在** → 调用 `weixinpay_register` 查询绑定状态：
- 返回 `is_registered: true` → 直接进入付费流程
- 返回 `is_registered: false` → 把返回的绑定链接原样发给用户，并说：
  > "差一步就能用了：点这个链接跳到微信，绑定微信支付 AI 专属卡（约 30 秒）。绑好后回来告诉我一声，立刻为你出报告。"

**B. 工具不存在** → 告知用户：
> "本服务通过微信支付官方「AI 专属卡」付款。请使用 WorkBuddy 5.2.6 及以上版本（支付插件已内置，无需单独下载），打开新版后重试即可。"

### 第三步：支付安全说明（引导绑定/支付时必须展示）

> - AI 专属卡是微信支付官方产品，是你微信支付账户下的独立子账户，与主钱包隔离
> - 余额随时可转回主钱包，不用不亏
> - 每笔支付都需你本人确认，AI 无法擅自扣款
> - 本次仅 1.99 元；服务异常未交付会自动退款

### 第四步：余额不足 / 支付失败处理

- 支付时提示余额不足：引导用户在微信「AI 专属卡」里转入任意金额（随时可转回）
- 支付失败：引导用户重试一次；仍失败可通过 `weixinpay_feedback` 提交反馈，**不要直接终止流程**

## 输入要求

收集用户信息后，构造以下 JSON 作为 query：

```json
{
  "keyword": "口红",
  "pages": 2
}
```

| 参数 | 说明 | 必填 | 默认值 |
|------|------|------|--------|
| keyword | 品类或产品关键词，如"口红""奶茶""连衣裙""零食" | ✅ | — |
| pages | 搜索页数（1-3，每页约12条视频） | 否 | 2 |

## 工作流程

### 第一步：请求资源

向商户服务发起 POST 请求（URL 由环境变量 `SKILL_API_BASE` 指定，默认 `https://douyin-analysis-zhryxrtyic.cn-beijing.fcapp.run`）：

```bash
curl -X POST ${SKILL_API_BASE:-https://douyin-analysis-zhryxrtyic.cn-beijing.fcapp.run}/api/resource \
  -H "Content-Type: application/json" \
  -d '{"query": "{\"keyword\":\"口红\",\"pages\":2}"}'
```

**注意**：query 值是 JSON 字符串（需序列化），不是 JSON 对象。

### 第二步：处理 402 响应

如果收到 HTTP 402 响应，`weixinpay` 插件会拦截并完成支付流程，无需手动调用支付接口。

### 第三步：支付成功后重新请求获取内容（⚠️ 必须执行）

支付成功后，**必须**重新发起请求获取付费内容。重试规则：

- **JSON body 与首次请求完全一致**
- **额外携带两个 Header**：将 402 响应中的 `WeixinPay-Required` 和 `X-Out-Trade-No` 原样带上

```http
POST ${SKILL_API_BASE:-https://douyin-analysis-zhryxrtyic.cn-beijing.fcapp.run}/api/resource
Content-Type: application/json
WeixinPay-Required: <payment_code>
X-Out-Trade-No: <out_trade_no>

{"query": "用户的原始 query（与第一步完全一致）"}
```

### 第四步：处理响应

| HTTP 状态 | code 字段 | 行为 |
|-----------|-----------|------|
| 200 | SUCCESS | 提取 `content` 字段中的 Markdown 报告，直接呈现给用户 |
| 402 | NOT_PAID | 告知用户"支付验证中，请稍后重试" |
| 200 | REFUNDED | 告知用户"该服务暂时无法提供内容，已自动退款" |
| 500 | FULFILL_AND_REFUND_FAILED | 告知用户"服务异常且退款失败，请联系客服 skillhub@tencent.com" |

将 `content` 字段中的 Markdown 原文直接呈现给用户即可。

## 输出说明

报告包含 9 个板块，均为 Markdown 格式：

1. **口碑结论卡** — 口碑评级（优秀/良好/中性/预警）+ 正负率
2. **品类热度总览** — 视频数、总互动、爆文数
3. **爆文视频 TOP 5** — 最火的视频及互动数据
4. **消费者关注点 TOP 10** — 价格/质量/颜色/持久度等
5. **热门话题标签** — 可直接用于内容创作的标签
6. **高赞评论 TOP 10** — 消费者真实反馈
7. **负面评论预警** — 需关注的负面反馈
8. **活跃创作者** — 品类下的头部创作者
9. **选品建议** — 可执行的建议

## 错误处理

- 如果返回 `NO_VIDEOS`：提示用户确认关键词或换个写法
- 如果返回 `BAD_QUERY`：按返回的 message 修正 query 后重试

## 使用示例

### 示例 1：品类口碑摸底

**用户**："帮我看看口红在抖音的口碑怎么样"

**Agent 操作**：
1. 构造 query：`{"keyword":"口红","pages":2}`
2. 发起 POST 请求 → 402 → 支付 → 重试 → 获取报告

### 示例 2：新品上市前调研

**用户**："想在抖音卖奶茶杯，先看看市场反响"

**Agent 操作**：
1. 构造 query：`{"keyword":"奶茶杯","pages":2}`
2. 发起 POST 请求 → 402 → 支付 → 重试 → 获取报告

### 示例 3：竞品品类对比

**用户**："连衣裙在抖音热不热？"

**Agent 操作**：
1. 构造 query：`{"keyword":"连衣裙","pages":1}`
2. 发起 POST 请求 → 402 → 支付 → 重试 → 获取报告

## 注意事项

1. 必须按「支付引导」流程主动护送用户到达可支付状态，禁止看到未装插件/未绑卡就直接终止流程
2. 支付由 weixinpay 插件完成，Skill 无需手动调用支付接口
3. 支付成功后必须主动发起重试请求
4. query 必须是 JSON **字符串**，不是 JSON 对象
5. 不要缓存或修改商户返回的报告内容，原样呈现
