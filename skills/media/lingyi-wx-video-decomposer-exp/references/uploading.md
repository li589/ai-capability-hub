# 上传视频的方法

> 何时读本文件：用户想拆解视频但尚未提供本地视频附件，需要引导其上传时。

若客户端的文件附件入口不支持直接选择视频文件，可以这样上传：

1. 在电脑文件夹里**复制**目标视频文件（右键 → 复制，或 ⌘C）；
2. 回到对话框输入框，**直接粘贴**（⌘V / Ctrl+V），视频会作为附件上传成功；
3. 上传完成后发送“拆解这个视频”即可。

agent 行为：用户想拆解但**当前还没有视频附件**时，先给出上面这段引导并停下，等用户上传后再继续。

> 上述引导为通用中性文案，不绑定具体客户端。Agent 无法自动侦测宿主客户端，故不做条件判断。
