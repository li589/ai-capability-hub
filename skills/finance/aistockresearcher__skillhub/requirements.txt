# stock-researcher 完整依赖
# 分层安装：
#   零依赖:   无需安装任何包 (python scripts/stock_predict.py)
#   MCP模式:  pip install mcp
#   量化模式:  pip install numpy pandas scipy scikit-learn statsmodels arch lightgbm empyrical
#   完整模式:  pip install -r requirements.txt
# 国内用户推荐清华镜像: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# === MCP SDK（启动 MCP Server 必需）===
mcp>=1.0.0

# === 核心数据处理 ===
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
requests>=2.31.0

# === 可视化与导出 ===
matplotlib>=3.7.0
openpyxl>=3.1.0
python-docx>=1.0.0

# === 量化分析（可选，缺失时对应工具返回友好提示而非崩溃）===
empyrical>=0.5.5
arch>=6.0.0
statsmodels>=0.14.0
scikit-learn>=1.3.0
lightgbm>=4.1.0

# === akshare（v8.0 可选数据源：基金净值/债券/可转债/宏观/港美股财务，未安装自动降级）===
akshare>=1.12.0

# === LLM（可选）===
ollama>=0.3.0
