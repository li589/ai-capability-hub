[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers --tb=short -q -p no:cacheprovider
filterwarnings =
markers =
    slow: 慢速测试（需要联网或重计算）
    integration: 集成测试（需要外部服务）
