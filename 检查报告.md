# AI 能力整合包 · 发布版检查报告（能力优先 v2）

生成时间：2026-08-21
检查对象：`Result/AI能力整合包_发布版/`

## 一、规模核对

| 项 | 值 |
|---|---|
| 能力类型数 | 9 |
| 包总数 | 3,510（manifest.csv 行数，0 悬空）|
| 文件总数 | 46,725 |
| 来源工具 | qoder-work 2012 · workbuddy 1371 · trae-cn 99 · trae 19 · qoder 6 · qoder-cn 3 |

## 二、分类质量

采用"能力优先"结构，顶层 9 类，二级统一为 21 类词表。

- `other` 杂项桶：**346 → 23**（原 346 个 other 包中 326 个已按扩展关键词规则重分类到明确类别）。
  - 残余 23 个均属真·杂项（生活/个人/随机工具、专家人设、画布模板、MCP 服务器），诚实保留。
- 跨工具同名冲突：以 `_<工具码>` 后缀并存，manifest 记录 `source_tool` 溯源。
- `mcps/` 为本次新增能力顶层（恢复 trae 的 5 个 MCP 包）。

## 三、安全复查（全量扫描 46,725 文件）

| 检查项 | 结果 |
|---|---|
| 已知密钥片段 / API Key / Token | 0 |
| 用户机器路径 `C:\Users\likr` · `/Users/likr` · `D:\...` | 0 |
| 用户自建标记 `agent_created: true`（frontmatter / HTML 注释）| 0* |
| 垃圾文件 `__pycache__` / `*.pyc` / `.DS_Store` / `*.log` / `*.tmp` | 0 |

\* 另有 2 处 `agent_created: true` 字样出现在 `README.md`（本说明文档）与 `skills/writing/.../persona-template.md`（人设模板示例字段），均为示例/文档文本，非真实技能 frontmatter，属误报，不计入自建技能。

## 四、恢复与排除核对

| 操作 | 结果 |
|---|---|
| 恢复 trae 全量 MCP | ✅ 新建 `mcps/`，并入 5 个包（1532 文件）；trae skills 此前已完整纳入 |
| trae-cn devops | ✅ `plugins/devops/trae-remote-official`（4496 文件）保留；清理厂商路径 `/Users/bytedance/...`→`/home/bytedance`；删除恢复中误生成的 `_tcn` 重复副本 |
| 排除用户个人项目能力 | ✅ 恢复源扫描：自建标记 0 / 项目名(CGDA 等) 0 / 真实机器路径 0 / 密钥 0；仅纳入通用工具能力 |

## 五、完整性核对

- manifest.csv 每行 `target_path` 均指向磁盘真实存在的包：**0 悬空**。
- 物理包数（按目录统计）与 manifest 行数一致（3,510）。
- 无包丢失：细化移动 + 恢复后包数 = 前版 3,504 + 新增 mcps 5 + devops 已计入 = 3,510，与磁盘一致。

## 六、遗留项（可选）

1. `mcps/other` 含 3 个 MCP 服务器（s_Clawdroid / s_gee / s_mat2py），属 MCP 类工具，暂留 other。
2. `skills/other` 残余 20 个为生活/个人/随机类技能，无强制归类意义。
3. 旧版备份目录 `Result/zz_old_publish_backup/`（若存在）可手动删除。
